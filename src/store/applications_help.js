export default class Application {
    constructor(
        application,
        roles,
        user,
        id
    ) {
            this.application = application
            this.roles = roles
            this.user = user
            this.id = id
    }
}